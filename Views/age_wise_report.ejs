<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Agewise Pending Applications</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <style>
        a{
            text-decoration: none;
        }
    </style>
  <div class="container mt-5">
    <h3 class="text-center mb-4"> Agewise Pending Applications</h3>
    <p class="text-muted text-center">Pending Applications from Application Submitted to Government</p>

    <table class="table table-bordered table-striped text-center">
        <p>Employee <b><%= name %></b></p>
      <thead class="table-dark">
        <tr>
          <th>Ref No</th>
          <th>Submitted to Govt.Portal </th>
          <th>Application Status</th>
          <th>< 15 days</th>
          <th>15-30 days</th>
          <th>31-45 days</th>
          <th>46-60 days</th>
          <th>&gt;60 days</th>
        </tr>
      </thead>
      <tbody>
        <% result.forEach((row, index) => { %>
          <tr>
            <td class="refno"><a href="/govt_process_dashboard"><%= row.ref_no %></a></td>
            <td><%=new Date(row.submitted_to_gov).toLocaleDateString("en-GB").split(",") %></td>
            <td><%if(row.sts==="Completed"){ %>
                <span class="badge bg-success">Completed</span>
               <%}else {%>
                <span class="badge bg-warning">Pending</span>    
               <%}%>
            </td>
            <td><%= row.moreThan15 || '' %></td>
            <td><%= row.day15to30 || '' %></td>
            <td><%= row.day31to45 || '' %></td>
            <td><%= row.day46to60 || '' %></td>
            <td><%= row.moreThan60 || '' %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
</div>
  <script> 
  

  </script>
</body>
</html>
